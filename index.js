var app =new Vue({
            el: '#app',
            data: {  
              message: 'Hello Vue.js!',
              test: 'desde la web',
              other: 'EXPER MODE'
            }
            /* for (const i in data){
            console log (`${i}: ${data[i]}`)
            } */
    
  });

app.other = "XXXXX";